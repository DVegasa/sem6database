import {ref} from "vue";

const data = ref([])


const updateAll = async () => {

}

const update = ($tableName) => {

}

export const useStore = () => {
  return {
    ref,
    updateAll,
    update,
  }
}
